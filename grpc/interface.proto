syntax = "proto3";

package token;
option go_package = "https://github.com/luczito/disys-handin4;token";

service Ring{
    rpc ring(Send) returns (Ack){}
}

message Send{
    bool access = 1;
}

message Ack{
    string reply = 1;
}